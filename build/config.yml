# This file contains the configuration for this project.
# When you update `info` or `fileAssociations`, run `wails3 task common:update:build-assets` to update the assets.
# Note that this will overwrite any changes you have made to the assets.
version: '3'

# This information is used to generate the build assets.
info:
  companyName: "Quad4 Software"
  productName: "Melovian"
  productIdentifier: "com.melovian.app"
  description: "Music player for local libraries and Subsonic-compatible servers"
  copyright: "Copyright 2026 Quad4 Software"
  comments: "Desktop music client with MPRIS support"
  version: "0.1.0"
  cfBundleIconName: "appicon"

# iOS build configuration
# Keys under `ios` override values under `info` when set.
ios:
  bundleID: "com.melovian.app"
  displayName: "Melovian"
  version: "0.1.0"
  company: "Quad4 Software"
  comments: "Music player for local libraries and Subsonic-compatible servers"
  backgroundModes:
    - audio

# Dev mode configuration
dev_mode:
  root_path: .
  log_level: warn
  debounce: 1000
  ignore:
    dir:
      - .git
      - node_modules
      - frontend
      - bin
    file:
      - .DS_Store
      - .gitignore
      - .gitkeep
      - "*_test.go"
    watched_extension:
      - "*.go"
      - "*.js" # Watch for changes to JS/TS files included using the //wails:include directive.
      - "*.ts" # The frontend directory will be excluded entirely by the setting above.
    git_ignore: true
  executes:
    - cmd: wails3 build DEV=true
      type: blocking
    - cmd: wails3 task common:dev:frontend
      type: background
    - cmd: wails3 task run
      type: primary

# File Associations
# More information at: https://v3.wails.io/noit/done/yet
fileAssociations:
#  - ext: wails
#    name: Wails
#    description: Wails Application File
#    iconName: wailsFileIcon
#    role: Editor
#  - ext: jpg
#    name: JPEG
#    description: Image File
#    iconName: jpegFileIcon
#    role: Editor
#    mimeType: image/jpeg  # (optional)

# Other data
other:
  - name: My Other Data